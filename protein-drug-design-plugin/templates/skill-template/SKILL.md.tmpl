---
name: skill-name-template
description: Use when [具体触发条件与症状——description 只写"何时使用"，不要总结技能的工作流程；中英文触发词都要埋；≤1024 字符]
---

# 技能名称 (English Name)

> 新技能创建步骤：复制本模板目录到 skills/<skill-name>/（目录名=技能名，小写连字符），
> 改名 frontmatter 的 name，删除本注释块。

## 0. 何时使用 / 不使用

- 使用：[症状列表]
- 不使用（转交其他技能）：[边界列表，指向对应技能名]

## 1. 核心流程（30 秒版）

```text
S1 ... → S2 ... → S3 ...
```

关键默认值表（实证出处）：

| 项 | 默认值 | 出处 |
|---|---|---|

## 2. 详细操作

（简单内容写这里；超过 ~50 行的重参考资料移到 references/*.md 并在此链接；确定性操作写成脚本放 scripts/ 并注明官方工具优先）

## 3. 高频坑速查

| 坑 | 规则 |
|---|---|

## 4. 产出清单

```text
<project>/
├── ...
```
